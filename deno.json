{
  "tasks": {
    "run": "export env=dev && export SERVER_KEY=test_12345 && cd ../ && deno run -A --watch ./space/index.js",
    "dev": "export env=dev && cd ../ && deno run -A --watch --unstable ./space/index.js"
  }
}
